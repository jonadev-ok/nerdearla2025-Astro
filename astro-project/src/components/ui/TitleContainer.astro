---
const { headingLevel = "h2", className = "" } = Astro.props;
const Tag = headingLevel;
---

<Tag
  class={`flex justify-center text-3xl font-semibold text-white ${className}`}
>
  <slot />
</Tag>
